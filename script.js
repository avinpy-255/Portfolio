function toggleMobileMenu() {
    document.getElementById("menu").classList.toggle("active");
}

for (let i = 0; i < fromInput.length; i ++) {
    fromInputs[i].addEventListener("input", function()
    {
        if (FormData.checkValidity())
    }
}